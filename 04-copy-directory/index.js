const fs = require('fs');
const path = require('path');
const copiedFolderPath = './04-copy-directory/files';
const targetFolderpath = './04-copy-directory/files-copy';

async function copyDir(scr, targetSrc) {

}
